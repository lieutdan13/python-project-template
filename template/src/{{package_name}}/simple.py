def add_one(number: int|float) -> int|float:
    """Increment *number* by one.

    Args:
        number: The number to increment.

    Returns:
        Increment of *number*.
    """
    return number + 1
